@extends('admin.layout')
@section('title', 'Chi tiết yêu cầu bảo trì')
@section('content')
<section class="page-header"><div><p class="eyebrow">Maintenance detail</p><h2>{{ $maintenanceRequest->title }}</h2><p>{{ $maintenanceRequest->priority }} · {{ $maintenanceRequest->status }}</p></div><div class="d-flex gap-2"><a href="{{ route('admin.maintenance_requests.edit',$maintenanceRequest) }}" class="btn btn-primary">Sửa</a><a href="{{ route('admin.maintenance_requests.index') }}" class="btn btn-light-soft">Quay lại</a></div></section>
<div class="row g-3"><div class="col-lg-4"><div class="card h-100"><div class="card-body"><div class="fw-semibold mb-2">Thông tin xử lý</div><div><strong>Rạp:</strong> {{ $maintenanceRequest->cinema?->name }}</div><div><strong>Phòng:</strong> {{ $maintenanceRequest->auditorium?->name ?: 'Toàn rạp' }}</div><div><strong>Thiết bị:</strong> {{ $maintenanceRequest->equipment?->name ?: 'Không gắn thiết bị' }}</div><div><strong>Người tạo:</strong> {{ $maintenanceRequest->requester?->full_name ?: 'Chưa có' }}</div><div><strong>Mở lúc:</strong> {{ optional($maintenanceRequest->opened_at)->format('d/m/Y H:i') }}</div><div><strong>Đóng lúc:</strong> {{ optional($maintenanceRequest->closed_at)->format('d/m/Y H:i') ?: 'Chưa đóng' }}</div></div></div></div><div class="col-lg-8"><div class="card h-100"><div class="card-body"><div class="fw-semibold mb-2">Mô tả sự cố / lịch sử sửa chữa</div><div>{{ $maintenanceRequest->description ?: 'Chưa có mô tả' }}</div></div></div></div></div>
@endsection
