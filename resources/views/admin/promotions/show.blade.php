@extends('admin.layout')
@section('title', 'Chi tiết khuyến mãi')
@section('content')
<section class="page-header"><div><p class="eyebrow">Promotion detail</p><h2>{{ $promotion->name }}</h2><p>{{ $promotion->code }}</p></div><div class="d-flex gap-2"><a href="{{ route('admin.promotions.edit', $promotion) }}" class="btn btn-primary">Sửa</a><a href="{{ route('admin.coupons.create', ['promotion_id' => $promotion->id]) }}" class="btn btn-light-soft">Tạo voucher</a><a href="{{ route('admin.promotions.index') }}" class="btn btn-light-soft">Quay lại</a></div></section>
<div class="row g-3"><div class="col-lg-4"><div class="card h-100"><div class="card-body"><div class="fw-semibold mb-2">Rule</div><div><strong>Kiểu:</strong> {{ $promotion->promo_type }}</div><div><strong>Giá trị:</strong> {{ $promotion->promo_type === 'PERCENT' ? $promotion->discount_value.'%' : number_format($promotion->discount_value).'đ' }}</div><div><strong>Tự áp:</strong> {{ $promotion->auto_apply ? 'Có' : 'Không' }}</div><div><strong>Voucher:</strong> {{ $promotion->coupon_required ? 'Có' : 'Không' }}</div><div><strong>Khách hàng:</strong> {{ $promotion->customer_scope }}</div><div class="mt-2">{{ $promotion->description ?: 'Không có mô tả' }}</div></div></div></div>
<div class="col-lg-4"><div class="card h-100"><div class="card-body"><div class="fw-semibold mb-2">Phạm vi áp dụng</div><div><strong>Phim:</strong> {{ $promotion->movies->pluck('title')->implode(', ') ?: 'Tất cả' }}</div><div class="mt-2"><strong>Rạp:</strong> {{ $promotion->cinemas->pluck('name')->implode(', ') ?: 'Tất cả' }}</div><div class="mt-2"><strong>Khung giờ:</strong> {{ $promotion->show_start_from ?: '--' }} - {{ $promotion->show_start_to ?: '--' }}</div></div></div></div>
<div class="col-lg-4"><div class="card h-100"><div class="card-body"><div class="fw-semibold mb-2">Hiệu quả</div><div><strong>Lượt dùng:</strong> {{ $effectiveness['used_count'] }}</div><div><strong>Tổng giảm:</strong> {{ number_format($effectiveness['discount_total']) }}đ</div><div><strong>Voucher đã phát:</strong> {{ $effectiveness['coupon_issued'] }}</div><div><strong>Voucher đã dùng:</strong> {{ $effectiveness['coupon_redeemed'] }}</div></div></div></div></div>
@endsection
