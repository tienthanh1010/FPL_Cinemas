@extends('admin.layout')
@section('title', 'Chi tiết nhân sự')
@section('content')
<section class="page-header"><div><p class="eyebrow">Staff detail</p><h2>{{ $staff->full_name }}</h2><p>{{ $staff->staff_code }} · {{ $staff->cinema?->name }}</p></div><div class="d-flex gap-2"><a href="{{ route('admin.staff.edit',$staff) }}" class="btn btn-primary">Sửa</a><a href="{{ route('admin.staff.index') }}" class="btn btn-light-soft">Quay lại</a></div></section>
<div class="row g-3"><div class="col-lg-4"><div class="card h-100"><div class="card-body"><div class="fw-semibold mb-2">Thông tin cơ bản</div><div><strong>SĐT:</strong> {{ $staff->phone ?: 'Chưa có' }}</div><div><strong>Email:</strong> {{ $staff->email ?: 'Chưa có' }}</div><div><strong>Ngày vào làm:</strong> {{ optional($staff->hired_at)->format('d/m/Y') ?: 'Chưa có' }}</div><div><strong>Trạng thái:</strong> {{ $staff->status }}</div><div><strong>Vai trò:</strong> {{ $staff->roles->pluck('name')->implode(', ') ?: 'Chưa gán' }}</div></div></div></div>
<div class="col-lg-4"><div class="card h-100"><div class="card-body"><div class="fw-semibold mb-2">Ca làm được phân</div>@forelse($staff->shifts as $shift)<div class="border-bottom py-2"><div>{{ optional($shift->shift_date)->format('d/m/Y') }} · {{ $shift->start_time }} - {{ $shift->end_time }}</div><div class="list-secondary">{{ $shift->note ?: 'Không có ghi chú' }}</div></div>@empty<div class="text-muted">Chưa được phân ca.</div>@endforelse</div></div></div>
<div class="col-lg-4"><div class="card h-100"><div class="card-body"><div class="fw-semibold mb-2">Yêu cầu bảo trì đã tạo</div>@forelse($staff->maintenanceRequests as $request)<div class="border-bottom py-2"><div>{{ $request->title }}</div><div class="list-secondary">{{ $request->equipment?->name ?: 'Không gắn thiết bị' }} · {{ $request->status }}</div></div>@empty<div class="text-muted">Chưa có yêu cầu bảo trì.</div>@endforelse</div></div></div></div>
@endsection
